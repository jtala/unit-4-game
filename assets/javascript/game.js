
// There is a randomly generated number from 19-120.

var tumToTarget = 30;

$("#targNum").text(targNum);

// There are four crystals. On click each crystal generates a random number from 1-12.

    // The first click is stored in myScore.

        // Every crystal click after that adds to myScore.

        // if myScore > randScore. Reset the loop.













